# Extra APT packages for pve-oci-compose on a Proxmox VE node (dpkg).
# pct, pvesh, perl (PVE::Storage), and oci-registry-pull come with Proxmox — not listed here.
#
# Format follows openstack/bindep: <packagename> [platform:dpkg ...]
# https://docs.opendev.org/opendev/bindep/latest/
#
# Install every listed package with apt (from the repo root):
#
#   sudo apt-get update && sudo apt-get install -y $(awk '!/^[[:space:]]*#/ && NF {print $1}' bindep.txt)
#
# If you use the bindep tool itself:  bindep -f bindep.txt --brief --platform dpkg

jq [platform:dpkg]
python3 [platform:dpkg]
python3-yaml [platform:dpkg]
skopeo [platform:dpkg]
rsync [platform:dpkg]
